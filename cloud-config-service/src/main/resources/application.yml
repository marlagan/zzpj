server:
  port: 8888

spring:
  application:
    name: cloud-config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          ##aboslute path to config-repo directory
          search-locations: file:///C:/Users/Admin/Desktop/zzpj/config-repo
