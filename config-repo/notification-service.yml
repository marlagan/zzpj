server:
  port: 8084

info:
  app:
    name: "Purrsuit Notification Service"

datasource:
  url: jdbc:postgresql://localhost:5432/notification_db
  username: admin
  password: admin
  driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect