server:
  port: 8086

info:
  app:
      name: "Purrsuit User Service"

datasource:
  url: jdbc:postgresql://localhost:5432/user_db
  username: admin
  password: admin
  driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect