server:
  port: 8085

info:
  app:
    name: "Purrsuit Pet Service"
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pet_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

#  llm:
#    api:
#      url: https://api.openai.com/v1
#      key: ${LLM_API_KEY}
#
#  matching:
#    similarity:
#      threshold: 0.75